<!-- Navigation -->
<nav class="bg-white shadow-lg">
    <div class="container mx-auto px-6 py-4">
        <div class="flex items-center justify-between">
            <div class="flex items-center space-x-4">
                <a href="/" class="flex items-center space-x-3">
                    <div class="w-8 h-8 bg-botbee-accent rounded-lg flex items-center justify-center">
                        <span class="text-white font-bold text-sm">N</span>
                    </div>
                    <span class="text-xl font-bold font-poppins text-botbee-accent">NOMED</span>
                </a>
                <span class="text-sm text-gray-500">/ Botbee</span>
            </div>
            <div class="hidden md:flex items-center space-x-8">
                <a href="/" class="text-gray-600 hover:text-botbee-accent transition-colors">Inicio</a>
                <a href="#caracteristicas" class="text-gray-600 hover:text-botbee-accent transition-colors">Características</a>
                <a href="#demo" class="text-gray-600 hover:text-botbee-accent transition-colors">Demo</a>
                <a href="#contacto" class="text-gray-600 hover:text-botbee-accent transition-colors">Contacto</a>
            </div>
            <button class="bg-botbee-accent text-white px-6 py-2 rounded-lg hover:bg-blue-700 transition-colors">
                Crear Personaje
            </button>
        </div>
    </div>
</nav>

<!-- Hero Section -->
<section class="py-20 bg-gradient-to-br from-botbee-primary to-botbee-gradient-end">
    <div class="container mx-auto px-6">
        <div class="grid lg:grid-cols-2 gap-16 items-center">
            <!-- Character Section -->
            <div class="relative animate-slide-in-left">
                <div class="relative">
                    <!-- Bee Character -->
                    <div class="bee-character relative z-10">
                        <div class="w-80 h-80 mx-auto relative">
                            <!-- Bee Body -->
                            <div class="bee-body absolute bottom-0 left-1/2 transform -translate-x-1/2 w-32 h-48 bg-yellow-400 rounded-full relative">
                                <!-- Black stripes -->
                                <div class="absolute top-4 left-0 w-full h-3 bg-black rounded-full"></div>
                                <div class="absolute top-12 left-0 w-full h-3 bg-black rounded-full"></div>
                                <div class="absolute top-20 left-0 w-full h-3 bg-black rounded-full"></div>
                                <div class="absolute top-28 left-0 w-full h-3 bg-black rounded-full"></div>
                                
                                <!-- Bee Head -->
                                <div class="absolute -top-8 left-1/2 transform -translate-x-1/2 w-20 h-20 bg-yellow-400 rounded-full">
                                    <!-- Eyes -->
                                    <div class="absolute top-4 left-3 w-4 h-4 bg-white rounded-full flex items-center justify-center">
                                        <div class="w-2 h-2 bg-botbee-accent rounded-full"></div>
                                    </div>
                                    <div class="absolute top-4 right-3 w-4 h-4 bg-white rounded-full flex items-center justify-center">
                                        <div class="w-2 h-2 bg-botbee-accent rounded-full"></div>
                                    </div>
                                    <!-- Smile -->
                                    <div class="absolute top-8 left-1/2 transform -translate-x-1/2 w-6 h-3 border-b-2 border-black rounded-full"></div>
                                </div>
                                
                                <!-- Antenna -->
                                <div class="bee-antenna absolute -top-12 left-1/2 transform -translate-x-1/2 w-1 h-8 bg-black">
                                    <div class="absolute -top-2 left-1/2 transform -translate-x-1/2 w-3 h-3 bg-black rounded-full"></div>
                                </div>
                                
                                <!-- Wings -->
                                <div class="bee-wings absolute -top-4 -left-8 w-16 h-12 bg-white bg-opacity-80 rounded-full border-2 border-gray-300"></div>
                                <div class="bee-wings absolute -top-4 -right-8 w-16 h-12 bg-white bg-opacity-80 rounded-full border-2 border-gray-300" style="animation-delay: 0.5s;"></div>
                            </div>
                            
                            <!-- Floating particles -->
                            <div class="absolute top-10 left-10 w-2 h-2 bg-yellow-200 rounded-full opacity-60 animate-float"></div>
                            <div class="absolute top-20 right-16 w-2 h-2 bg-yellow-200 rounded-full opacity-60 animate-float" style="animation-delay: 1s;"></div>
                            <div class="absolute bottom-20 left-16 w-2 h-2 bg-yellow-200 rounded-full opacity-60 animate-float" style="animation-delay: 2s;"></div>
                        </div>
                    </div>
                    
                    <!-- Floating icons -->
                    <div class="absolute top-10 left-10 w-12 h-12 bg-white bg-opacity-20 rounded-full flex items-center justify-center animate-float">
                        <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"></path>
                        </svg>
                    </div>
                    
                    <div class="absolute top-32 right-16 w-8 h-8 bg-white bg-opacity-20 rounded-full flex items-center justify-center animate-float" style="animation-delay: 1s;">
                        <svg class="w-5 h-5 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9.663 17h4.673M12 3v1m6.364 1.636l-.707.707M21 12h-1M4 12H3m3.343-5.657l-.707-.707m2.828 9.9a5 5 0 117.072 0l-.548.547A3.374 3.374 0 0014 18.469V19a2 2 0 11-4 0v-.531c0-.895-.356-1.754-.988-2.386l-.548-.547z"></path>
                        </svg>
                    </div>
                    
                    <div class="absolute bottom-20 left-16 w-6 h-6 bg-white bg-opacity-20 rounded-full flex items-center justify-center animate-float" style="animation-delay: 2s;">
                        <svg class="w-4 h-4 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13 10V3L4 14h7v7l9-11h-7z"></path>
                        </svg>
                    </div>
                </div>
            </div>
            
            <!-- Content Section -->
            <div class="space-y-8 animate-slide-in-right">
                <div class="space-y-6">
                    <div class="inline-flex items-center space-x-2">
                        <div class="w-12 h-12 bg-white bg-opacity-20 rounded-xl flex items-center justify-center">
                            <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"></path>
                            </svg>
                        </div>
                        <span class="text-white text-sm font-medium">BOTBEE</span>
                    </div>
                    
                    <h1 class="text-4xl lg:text-6xl font-bold font-poppins text-white leading-tight">
                        Personajes interactivos para el aprendizaje dinámico
                    </h1>
                    
                    <p class="text-xl text-white text-opacity-90 leading-relaxed">
                        Crea personajes que plantean preguntas, explican conceptos y 
                        hacen que el aprendizaje sea más divertido y efectivo.
                    </p>
                </div>
                
                <div class="flex flex-col sm:flex-row gap-4">
                    <button class="bg-white text-botbee-primary px-8 py-4 rounded-lg text-lg font-semibold hover:bg-gray-100 transition-colors">
                        ¡Crea tu personaje interactivo ahora!
                    </button>
                    <button class="border-2 border-white text-white px-8 py-4 rounded-lg text-lg font-semibold hover:bg-white hover:text-botbee-primary transition-colors">
                        Ver Demo
                    </button>
                </div>
                
                <div class="text-white text-opacity-80">
                    <p class="text-sm">🤖 <strong>botbee.nomed.org</strong></p>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- Features Section -->
<section id="caracteristicas" class="py-20 bg-nomed-gray">
    <div class="container mx-auto px-6">
        <div class="text-center space-y-4 mb-16">
            <h2 class="text-3xl lg:text-4xl font-bold font-poppins text-botbee-accent">Características Principales</h2>
            <p class="text-xl text-gray-600 max-w-3xl mx-auto">
                Descubre todas las capacidades que Botbee pone a tu disposición
            </p>
        </div>
        
        <div class="grid md:grid-cols-3 gap-8">
            <% features.forEach((feature, index) => { %>
            <div class="feature-card bg-white rounded-2xl p-8 shadow-lg">
                <div class="w-16 h-16 bg-botbee-accent rounded-xl flex items-center justify-center mb-6">
                    <svg class="w-8 h-8 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <% if (feature.icon === 'settings') { %>
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6V4m0 2a2 2 0 100 4m0-4a2 2 0 110 4m-6 8a2 2 0 100-4m0 4a2 2 0 100 4m0-4v2m0-6V4m6 6v10m6-2a2 2 0 100-4m0 4a2 2 0 100 4m0-4v2m0-6V4"></path>
                        <% } else if (feature.icon === 'chat') { %>
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"></path>
                        <% } else { %>
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path>
                        <% } %>
                    </svg>
                </div>
                <h3 class="text-xl font-bold text-botbee-text mb-4"><%= feature.title %></h3>
                <p class="text-gray-600 leading-relaxed">
                    <%= feature.description %>
                </p>
            </div>
            <% }); %>
        </div>
    </div>
</section>

<!-- Interactive Demo Section -->
<section id="demo" class="py-20 bg-white">
    <div class="container mx-auto px-6">
        <div class="text-center space-y-4 mb-16">
            <h2 class="text-3xl lg:text-4xl font-bold font-poppins text-botbee-accent">Chatea con Botbee</h2>
            <p class="text-xl text-gray-600 max-w-3xl mx-auto">
                Prueba cómo funciona Botbee en tiempo real
            </p>
        </div>
        
        <div class="max-w-4xl mx-auto">
            <div class="bg-nomed-gray rounded-2xl p-8">
                <div class="space-y-6">
                    <!-- Chat Header -->
                    <div class="flex items-center space-x-4 pb-6 border-b border-gray-300">
                        <div class="w-12 h-12 bg-botbee-accent rounded-full flex items-center justify-center">
                            <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                                <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"></path>
                            </svg>
                        </div>
                        <div>
                            <h3 class="font-semibold text-botbee-accent">Botbee Asistente</h3>
                            <p class="text-sm text-gray-600">En línea • Responde en segundos</p>
                        </div>
                    </div>
                    
                    <!-- Chat Messages -->
                    <div id="chat-messages" class="space-y-4 min-h-64 max-h-96 overflow-y-auto">
                        <% demoMessages.forEach((message, index) => { %>
                        <div class="chat-message">
                            <div class="speech-bubble">
                                <p class="text-gray-700"><%= message.message %></p>
                            </div>
                        </div>
                        <% }); %>
                    </div>
                    
                    <!-- Quick Actions -->
                    <div class="space-y-3">
                        <p class="text-sm text-gray-600 font-medium">Respuestas rápidas:</p>
                        <div class="flex flex-wrap gap-2">
                            <button class="px-4 py-2 bg-white rounded-lg border border-gray-200 hover:border-botbee-accent transition-colors text-sm" onclick="sendMessage('Matemáticas')">
                                📐 Matemáticas
                            </button>
                            <button class="px-4 py-2 bg-white rounded-lg border border-gray-200 hover:border-botbee-accent transition-colors text-sm" onclick="sendMessage('Historia')">
                                📚 Historia
                            </button>
                            <button class="px-4 py-2 bg-white rounded-lg border border-gray-200 hover:border-botbee-accent transition-colors text-sm" onclick="sendMessage('Ciencias')">
                                🧪 Ciencias
                            </button>
                            <button class="px-4 py-2 bg-white rounded-lg border border-gray-200 hover:border-botbee-accent transition-colors text-sm" onclick="sendMessage('Explicar un concepto')">
                                💡 Explicar un concepto
                            </button>
                        </div>
                    </div>
                    
                    <!-- Input Area -->
                    <div class="flex space-x-4">
                        <input type="text" id="message-input" placeholder="Escribe tu pregunta aquí..." class="flex-1 px-4 py-3 border border-gray-300 rounded-lg focus:ring-2 focus:ring-botbee-accent focus:border-transparent">
                        <button onclick="sendMessage()" class="bg-botbee-accent text-white px-6 py-3 rounded-lg hover:bg-blue-700 transition-colors">
                            Enviar
                        </button>
                    </div>
                </div>
            </div>
        </div>
    </div>
</section>

<!-- CTA Section -->
<section class="py-20 bg-gradient-to-r from-botbee-accent to-blue-600 text-white">
    <div class="container mx-auto px-6 text-center">
        <div class="max-w-3xl mx-auto space-y-8">
            <h2 class="text-3xl lg:text-4xl font-bold font-poppins">
                ¿Listo para crear tu personaje educativo?
            </h2>
            <p class="text-xl opacity-90">
                Únete a miles de educadores que ya están usando Botbee para hacer el aprendizaje más interactivo
            </p>
            <div class="flex flex-col sm:flex-row gap-4 justify-center">
                <button class="bg-white text-botbee-accent px-8 py-4 rounded-lg text-lg font-semibold hover:bg-gray-100 transition-colors">
                    Crear Personaje Gratis
                </button>
                <button class="border-2 border-white text-white px-8 py-4 rounded-lg text-lg font-semibold hover:bg-white hover:text-botbee-accent transition-colors">
                    Ver Tutorial
                </button>
            </div>
        </div>
    </div>
</section>

<style>
    .bee-character {
        position: relative;
        animation: dance 2s ease-in-out infinite;
    }
    
    .bee-body {
        animation: buzz 0.5s ease-in-out infinite;
    }
    
    .bee-wings {
        animation: float 1s ease-in-out infinite;
    }
    
    .bee-antenna {
        animation: wiggle 2s ease-in-out infinite;
    }
    
    .speech-bubble {
        position: relative;
        background: white;
        border: 3px solid #009DE6;
        border-radius: 20px;
        padding: 20px;
        box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
        animation: talking 0.5s ease-in-out infinite alternate;
    }
    
    .speech-bubble::after {
        content: '';
        position: absolute;
        bottom: -15px;
        left: 30px;
        width: 0;
        height: 0;
        border-left: 15px solid transparent;
        border-right: 15px solid transparent;
        border-top: 15px solid #009DE6;
    }
    
    .speech-bubble::before {
        content: '';
        position: absolute;
        bottom: -12px;
        left: 33px;
        width: 0;
        height: 0;
        border-left: 12px solid transparent;
        border-right: 12px solid transparent;
        border-top: 12px solid white;
    }
    
    .chat-message {
        animation: slideInLeft 0.5s ease-out;
    }
    
    .user-message {
        animation: slideInRight 0.5s ease-out;
    }
    
    .typing-indicator {
        animation: fadeIn 0.5s ease-in-out infinite alternate;
    }
    
    .feature-card {
        transition: all 0.3s ease;
    }
    
    .feature-card:hover {
        transform: translateY(-5px);
        box-shadow: 0 20px 40px rgba(0, 0, 0, 0.1);
    }
</style>

<script>
    let messageCount = 0;
    
    function sendMessage(customMessage = null) {
        const input = document.getElementById('message-input');
        const message = customMessage || input.value.trim();
        
        if (!message) return;
        
        const chatMessages = document.getElementById('chat-messages');
        
        // Add user message
        const userMessage = document.createElement('div');
        userMessage.className = 'user-message flex justify-end';
        userMessage.innerHTML = `
            <div class="bg-botbee-accent text-white rounded-2xl px-6 py-3 max-w-xs">
                <p>${message}</p>
            </div>
        `;
        chatMessages.appendChild(userMessage);
        
        // Clear input if not a quick action
        if (!customMessage) {
            input.value = '';
        }
        
        // Show typing indicator
        const typingIndicator = document.createElement('div');
        typingIndicator.className = 'chat-message';
        typingIndicator.innerHTML = `
            <div class="typing-indicator flex items-center space-x-2">
                <div class="w-8 h-8 bg-gray-300 rounded-full flex items-center justify-center">
                    <div class="w-3 h-3 bg-gray-500 rounded-full animate-bounce"></div>
                </div>
                <div class="bg-white rounded-2xl px-6 py-3">
                    <div class="flex space-x-1">
                        <div class="w-2 h-2 bg-gray-400 rounded-full animate-bounce"></div>
                        <div class="w-2 h-2 bg-gray-400 rounded-full animate-bounce" style="animation-delay: 0.1s;"></div>
                        <div class="w-2 h-2 bg-gray-400 rounded-full animate-bounce" style="animation-delay: 0.2s;"></div>
                    </div>
                </div>
            </div>
        `;
        chatMessages.appendChild(typingIndicator);
        
        // Scroll to bottom
        chatMessages.scrollTop = chatMessages.scrollHeight;
        
        // Send to API
        fetch('/api/botbee/chat', {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
            },
            body: JSON.stringify({ message })
        })
        .then(response => response.json())
        .then(data => {
            // Remove typing indicator
            typingIndicator.remove();
            
            if (data.success) {
                const botMessage = document.createElement('div');
                botMessage.className = 'chat-message';
                botMessage.innerHTML = `
                    <div class="speech-bubble">
                        <p class="text-gray-700">${data.response}</p>
                    </div>
                `;
                chatMessages.appendChild(botMessage);
            } else {
                const errorMessage = document.createElement('div');
                errorMessage.className = 'chat-message';
                errorMessage.innerHTML = `
                    <div class="speech-bubble">
                        <p class="text-gray-700">Lo siento, hubo un error. Inténtalo de nuevo.</p>
                    </div>
                `;
                chatMessages.appendChild(errorMessage);
            }
            
            // Scroll to bottom
            chatMessages.scrollTop = chatMessages.scrollHeight;
            
            messageCount++;
            
            // Add follow-up suggestions after a few messages
            if (messageCount === 3) {
                setTimeout(() => {
                    const followUp = document.createElement('div');
                    followUp.className = 'chat-message';
                    followUp.innerHTML = `
                        <div class="speech-bubble">
                            <p class="text-gray-700">¿Te gustaría que creemos un personaje personalizado para tu clase? ¡Puedo adaptarme a cualquier materia y nivel educativo! 🎓</p>
                        </div>
                    `;
                    chatMessages.appendChild(followUp);
                    chatMessages.scrollTop = chatMessages.scrollHeight;
                }, 2000);
            }
        })
        .catch(error => {
            console.error('Error:', error);
            typingIndicator.remove();
            
            const errorMessage = document.createElement('div');
            errorMessage.className = 'chat-message';
            errorMessage.innerHTML = `
                <div class="speech-bubble">
                    <p class="text-gray-700">Lo siento, hubo un error de conexión. Inténtalo de nuevo.</p>
                </div>
            `;
            chatMessages.appendChild(errorMessage);
            chatMessages.scrollTop = chatMessages.scrollHeight;
        });
    }
    
    // Handle Enter key
    document.getElementById('message-input').addEventListener('keypress', function(e) {
        if (e.key === 'Enter') {
            sendMessage();
        }
    });
</script>
